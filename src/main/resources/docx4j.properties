# Present only to silence docx4j's own startup complaint ("Couldn't
# find/read docx4j.properties") - it looks this file up on the classpath and
# warns twice when it's absent, even though every setting in it is optional
# and this app is happy with all the defaults.
#
# Add real settings here if ever needed - docx4j's own
# docx4j-core/src/main/resources/docx4j.properties documents the full set
# (unzip limits, XSLT/XPath factories, docx4j-export-fo config, and so on).
